# Define variables
LOGS_DIR=/fast/najroldi/logs/algoperf
EXE=/home/najroldi/algorithmic-efficiency/exp/unified/lawa_cpu/condor/auto_run_array.sh

# Job specific vars
+JobName=imagenet_resnet
dataset=imagenet
workload=imagenet_resnet
submission=submissions/lawa_cpu/lawa.py
search_space=exp/unified/lawa_cpu/json/baseline/resnet.json
exp_name=lawa_cpu
study=1
num_tuning_trials=5

# Args
executable = $(EXE)
arguments = \
  $(dataset) \
  $(workload) \
  $(submission) \
  $(search_space) \
  $(exp_name) \
  $(study) \
  $(num_tuning_trials) \
  $(Process) \
  $(rng_seed)

# Logs
error = $(LOGS_DIR)/err/job.$(Cluster).$(Process).err
output = $(LOGS_DIR)/out/job.$(Cluster).$(Process).out
log = $(LOGS_DIR)/log/job.$(Cluster).$(Process).log

# Specs
request_memory = 500000
request_cpus = 16
request_gpus = 2
requirements = (TARGET.CUDADeviceName == "NVIDIA A100-SXM4-80GB")

queue $(num_tuning_trials)