# Define variables
LOGS_DIR=/fast/najroldi/logs/algoperf
EXE=/home/najroldi/algorithmic-efficiency/exp/unified/lawa_trick_check_cpu_ram/condor/auto_run_array.sh

# Job specific vars
dataset=criteo1tb
workload=criteo1tb
search_space=exp/unified/lawa_trick_check_cpu_ram/json/trial_5 copy.json

# Submission
submission=submissions/lawa_trick_bf16/lawa.py
exp_name=lawa_trick_bf16_01
study=1
num_tuning_trials=1
rng_seed=1

# Args
executable = $(EXE)
arguments = \
  $(dataset) \
  $(workload) \
  $(submission) \
  $(search_space) \
  $(exp_name) \
  $(study) \
  $(num_tuning_trials) \
  $(Process) \
  $(rng_seed)

# Logs
error = $(LOGS_DIR)/err/job.$(Cluster).$(Process).err
output = $(LOGS_DIR)/out/job.$(Cluster).$(Process).out
log = $(LOGS_DIR)/log/job.$(Cluster).$(Process).log

# Specs
request_memory = 240000
request_cpus = 28
request_gpus = 8
requirements = (TARGET.CUDADeviceName == "Tesla V100-PCIE-16GB")

queue $(num_tuning_trials)